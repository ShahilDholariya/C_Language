#include <stdio.h>
#include <stdlib.h>

int main() {

   int dividend = 20;
   int divisor = 0;
   int result;
   
   result = dividend / divisor;
   printf("Value is : %d\n", result );

   exit(-1);
}